---
name: jonathanleahy/kart-circuit
purpose: Browser kart racer — Three.js + TypeScript with a deterministic fixed-timestep arcade physics core, drift-boost, and a solo time-trial on a closed circuit.
layer: product
status: active
owner: "jonathanleahy"
orientation_version: 350de5e
generated_by: gyrum-orient
---
<!-- generated by gyrum-orient from repo-meta.yaml + repo scan.
     DO NOT EDIT BY HAND — run `gyrum-orient` to regenerate.
     Kept honest by the orientation-drift gate in gyrum-review-pr. -->

# jonathanleahy/kart-circuit — Orientation

## Purpose

Browser kart racer — Three.js + TypeScript with a deterministic fixed-timestep arcade physics core, drift-boost, and a solo time-trial on a closed circuit.

## Stack

- Language/runtime: Node.js / JavaScript
- Build: tsc && vite build

## Run & test

- Run: vite
- Test: npm test

## Entry points

- (none detected)

## Conventions & gates

- See CLAUDE.md and the repo's review gates for conventions.
- ORIENTATION.md is generated; do not hand-edit (run gyrum-orient).

## Raising work

Tickets in this repo follow the persona-driven raising contract (warp#11068):

1. **Frame with Jan.** Before filing, run the Jan requirements persona to
   frame the problem, acceptance criteria, non-goals, and scope boundary.
   Jan frames the WHAT, never the HOW. The requirements section must be
   implementation-free.
2. **Sign the attestation.** The raising agent signs an attestation
   (agent-id + role + timestamp + requirements-hash) over the framed
   requirements. Editing the requirements after signing invalidates the
   hash (drift is detectable).
3. **The acceptance criteria are the contract.** warp-complete invokes
   Jan to validate every AC is met and the requirements hash hasn't
   drifted since attestation. Unmet criteria or a drifted hash blocks
   completion.
4. **Lint before filing.** `warp-add --lint <file>` runs the five
   raising gates (requirements-present, persona-used,
   no-impl-in-requirements, criteria-observable, signed) plus the
   existing author-time gates. Fix all gate failures before filing.

## Links

- [Live repo](https://github.com/jonathanleahy/kart-circuit)
